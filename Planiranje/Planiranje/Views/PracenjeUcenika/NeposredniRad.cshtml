@model Planiranje.Models.Ucenici.PracenjeUcenikaModel
@{
    Layout = null;
}
<p>
    <a onclick="showModal('@Url.Action("DodajNeposredniRad","PracenjeUcenika", new { id=Model.Ucenik.Id_ucenik})')" class="btn btn-primary btn-xs btn-grad"> Novi zapis</a>
</p>
<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover" id="dataTable3">
        <thead>
            <tr>
                <th>Datum</th>
                <th>Oblici i način rada s učenikom, učiteljima, roditeljima/starateljima i drugima </th>
                <th>Aktivnost</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.NeposredniRadovi)
            {
            <tr>
                <td class="col-md-1">@item.Datum.ToShortDateString()</td>
                <td class="col-md-9">@item.Napomena</td>
                <td class="col-md-2">
                    <a href="#" class="btn btn-primary btn-xs btn-grad">Uredi</a>
                    <a href="#" class="btn btn-danger btn-xs btn-grad">Briši</a>
                </td>
            </tr>           
            }
        </tbody>
    </table>
</div>
